spring.application.name=nunki

# OPC UA Configuration
quasar.opcua.url=${QUASAR_OPCUA_URL:opc.tcp://localhost:4840}
opcua.client.endpoint-url=${quasar.opcua.url}
opcua.embedded-server.enabled=${OPCUA_EMBEDDED_SERVER_ENABLED:false}
opcua.embedded-server.port=${OPCUA_EMBEDDED_SERVER_PORT:4840}

